import * as React from "react";
import {useState, useEffect} from 'react'
const Home  = () => {

//the && symbols allow us to cause a delay which waits fro the data to be recieved before we move foward with the rest of the code to avoid null errors
    return (
          <div className="home">
              
          </div>

    );
}

export default Home ;